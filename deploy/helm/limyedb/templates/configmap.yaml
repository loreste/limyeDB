apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "limyedb.fullname" . }}-config
  labels:
    {{- include "limyedb.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- .Values.config | toJson | nindent 4 }}
